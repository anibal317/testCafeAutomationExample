import { t } from 'testcafe';

export async function login(username, password) {
    // Implementa aquí una función de inicio de sesión reutilizable que utiliza el Page Object LoginPage
    // Puedes usar esta función en diferentes pruebas para iniciar sesión con diferentes credenciales.
}
